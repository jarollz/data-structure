package listlinkeddoubly

// New creates empty doubly linked list.
//
// Returned list has Len() == 0 with empty head and tail state and is ready for
// O(1) end operations defined by the API.
//
// Example: s := New[int]()
func New[T any]() *ListLinkedDoubly[T] {
	return nil
}
