package treegeneral

// New creates general tree with one root node.
//
// rootValue is stored at root node, and root node ID is always 0.
//
// Example: tr := New[string]("root")
func New[T any](rootValue T) *TreeGeneral[T] {
	return nil
}
